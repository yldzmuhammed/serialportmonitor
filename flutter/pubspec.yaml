name: serial_port_monitor
description: Cross-platform serial port monitor with embedded MCP server
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_libserialport: ^0.4.0
  file_selector: ^1.0.3
  path_provider: ^2.1.4

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/oui.csv
